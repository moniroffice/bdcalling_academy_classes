// Exporting functions using ES6 syntax
export function add(a, b) {
  return a + b;
}

export function multiply(a, b) {
  return a * b;
}

// Default export (only one allowed per file)
export default function subtract(a, b) {
  return a - b;
}
